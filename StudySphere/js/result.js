const attemptId = new URLSearchParams(window.location.search).get("attempt");
const stored = JSON.parse(localStorage.getItem(`studysphere_result_${attemptId}`) || "null");
const card = document.querySelector("#result-card");
if (!stored) { card.innerHTML = '<h1>Không tìm thấy kết quả</h1><p class="detail-description">Kết quả có thể đã bị xóa khỏi trình duyệt.</p><a class="button button-primary" href="exams.html">Về kho đề</a>'; } else { const { result } = stored; card.innerHTML = `<p class="eyebrow"><span class="eyebrow-dot"></span> Bài làm đã hoàn tất</p><h1>${result.score}<small>/10 điểm</small></h1><p class="detail-description">${result.examTitle}</p><div class="detail-stats"><div><strong>${result.correctCount}</strong><span>Câu đúng</span></div><div><strong>${result.wrongCount}</strong><span>Câu sai</span></div><div><strong>${result.unansweredCount}</strong><span>Bỏ qua</span></div></div><div class="detail-actions"><a class="button button-primary" href="quiz.html?id=${result.examId}">Làm lại bài</a><a class="button button-quiet" href="exams.html">Về kho đề</a></div>`; }
